<!doctype html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>Brf Bronsyxan – {{title}}</title>
		<link rel="stylesheet" href="./style.css" />
		{{#if dev}}
		<script src="http://localhost:35729/livereload.js"></script>
		{{/if}}
	</head>
	<body>
		{{> header}}
		<section class="content content-{{type}}">
		{{{contents}}}
		</section>
		{{> footer }}
	</body>
</html>